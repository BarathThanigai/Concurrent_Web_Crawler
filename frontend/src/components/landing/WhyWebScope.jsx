export default function WhyWebScope() {
  return (
    <section className="landing-section">
      <div className="landing-section-header">
        <p className="eyebrow">Why WebScope</p>
        <h2>A focused crawler for portfolio-grade auditing</h2>
      </div>
      <p className="landing-copy">
        WebScope keeps crawling bounded, respects robots.txt, classifies crawler accessibility issues,
        and presents crawl intelligence in a clean dashboard instead of a raw URL dump.
      </p>
    </section>
  );
}
